name: Test and Sync Wazuh Rules

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate-and-sync:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install XML validator
        run: sudo apt-get update && sudo apt-get install -y libxml2-utils

      - name: Validate XML rules
        run: |
          if [ -d "rules" ]; then
            for file in rules/*.xml; do
              if [ -f "$file" ]; then
                echo "Validating $file"
                xmllint --noout "$file"
              fi
            done
          else
            echo "Folder rules tidak ditemukan!"
          fi

      - name: Detect changed files
        id: detect_files
        run: |
          # Ambil daftar file yang berubah
          FILES=$(git diff --name-only HEAD^ HEAD | grep -E 'rules/.*\.xml|decoders/.*\.xml' || true)
          
          if [ -z "$FILES" ]; then
            echo "CHANGED_FILES=No XML/Decoders changed" >> $GITHUB_ENV
            echo "HAS_CHANGES=false" >> $GITHUB_ENV
          else
            # Simpan daftar asli untuk proses upload
            echo "$FILES" > changed_files_list.txt
            
            # Format untuk tampilan Slack
            FORMATTED_FILES=$(echo "$FILES" | sed 's/^/- /' | tr '\n' ',' | sed 's/,$//' | sed 's/,/\\n/g')
            echo "CHANGED_FILES=$FORMATTED_FILES" >> $GITHUB_ENV
            echo "HAS_CHANGES=true" >> $GITHUB_ENV
          fi

      - name: Sync to Wazuh Cloud
        if: success() && env.HAS_CHANGES == 'true'
        run: |
          # 1. Dapatkan Token
          TOKEN=$(curl -u "${{ secrets.WAZUH_USER }}:${{ secrets.WAZUH_PASS }}" -k -X POST "https://tnr0wn663gdu.cloud.wazuh.com/api/wazuh/security/user/authenticate?raw=true")
          
          # 2. Upload setiap file yang berubah
          while IFS= read -r file; do
            filename=$(basename "$file")
            echo "Uploading $filename to Wazuh Cloud..."
            
            curl -k -X PUT "https://tnr0wn663gdu.cloud.wazuh.com/api/wazuh/rules/files/$filename?overwrite=true" \
                 -H "Authorization: Bearer $TOKEN" \
                 -H "Content-Type: application/octet-stream" \
                 --data-binary "@$file"
          done < changed_files_list.txt

          # 3. RESTART WAZUH MANAGER (Agar rule aktif)
          echo "Restarting Wazuh Manager..."
          curl -k -X PUT "https://tnr0wn663gdu.cloud.wazuh.com/api/wazuh/manager/restart" \
               -H "Authorization: Bearer $TOKEN"

      - name: Notify Slack
        if: always()
        env:
          SLACK_WEBHOOK: ${{ secrets.TEST_INT }}
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            EMOJI="✅"
          else
            EMOJI="❌"
          fi

          curl -X POST -H 'Content-type: application/json' \
          --data "{
            \"text\": \"$EMOJI *WAZUH RULES UPDATE*\n*Status:* ${{ job.status }}\n*Repo:* ${{ github.repository }}\n*Commit:* ${{ github.sha }}\n\n*Changed Files:*\n${{ env.CHANGED_FILES }}\"
          }" $SLACK_WEBHOOK
